# MVP-043: Work Items UI Module

**Status**: Not Started  
**Priority**: P1 (Critical)  
**Effort**: Medium  
**Skills Required**: Go, Templ, Frontend Dev, HTMX  
**Dependencies**: MVP-029 (Goals Module - provides foundation patterns)

---

## Objective

Build a complete Work Items management interface in the Agency Designer with CRUD operations, AI-powered refinement, smart templates, deliverables and dependencies management, and advanced filtering capabilities.

---

## Key Deliverables

### 1. Work Items Data Model

```go
type WorkItem struct {
    Key             string              `json:"key"`              // e.g., "WI-001"
    AgencyID        string              `json:"agency_id"`
    Title           string              `json:"title"`
    Description     string              `json:"description"`
    Type            WorkItemType        `json:"type"`             // Task, Feature, Epic, etc.
    Priority        Priority            `json:"priority"`         // P0, P1, P2, P3
    Status          WorkItemStatus      `json:"status"`           // Not Started, In Progress, Done
    Deliverables    []DeliverableNode   `json:"deliverables"`     // 🆕 Enhanced hierarchical structure
    Dependencies    []string            `json:"dependencies"`     // References to other work item keys
    EstimatedEffort string              `json:"estimated_effort"` // e.g., "2 weeks", "40 hours"
    AssignedTo      string              `json:"assigned_to,omitempty"`
    Tags            []string            `json:"tags,omitempty"`
    CreatedAt       time.Time           `json:"created_at"`
    UpdatedAt       time.Time           `json:"updated_at"`
}

// 🆕 Enhanced Deliverable Structure with Nested Folders
type DeliverableNode struct {
    ID                 string            `json:"id"`                   // UUID for UI tracking
    Name               string            `json:"name"`                 // "stakeholders.md" or "introduction"
    Description        string            `json:"description"`          // Brief description of deliverable
    Path               string            `json:"path"`                 // Computed full path
    Type               DeliverableType   `json:"type"`                 // "folder" or "file"
    PromptInstructions string            `json:"prompt_instructions"`  // AI instructions (what to achieve)
    Children           []DeliverableNode `json:"children,omitempty"`   // Nested deliverables
    FileExtension      string            `json:"file_extension"`       // ".md" (initially only markdown)
}

type DeliverableType string
const (
    DeliverableTypeFolder DeliverableType = "folder"
    DeliverableTypeFile   DeliverableType = "file"
)

// Validation Constraints
const (
    MaxNestingDepth     = 10    // Prevent excessive nesting
    MaxNameLength       = 255   // File/folder name limit
    MaxPromptLength     = 5000  // Prompt instruction limit
    MaxChildrenPerNode  = 100   // Prevent overly complex trees
)
```

**Key Features**:
- **Hierarchical Structure**: Deliverables organized as folder/file trees
- **Prompt Instructions**: Each node has AI instructions explaining what to achieve
- **Inheritance Model**: Child nodes inherit parent folder instructions
- **User-Defined**: Flexible structure defined by users (no enforced templates)
- **Markdown Focus**: Initially supports `.md` files only

### 2. Work Items List View

- Table/card view of all work items
- Filtering by status, priority, type, tags
- Search functionality
- Sortable columns (priority, status, created date)
- Bulk operations (delete, update status)
- Quick actions (edit, delete, duplicate)
- Color coding by priority and status

### 3. Work Item Editor (Modal/Form)

- Title and description fields
- Type selector (Task, Feature, Epic, Bug, Research)
- Priority selector (P0-P3)
- Status selector with workflow
- **🆕 Deliverables Tree Builder** (hierarchical folder/file editor with drag-and-drop)
  - Visual tree/folder view
  - Add Folder / Add File buttons
  - Drag-and-drop to reorder and nest items
  - Inline editing for names, descriptions, prompt instructions
  - Expand/collapse navigation
  - File type indicators (folder icon, markdown icon)
  - Path preview (auto-computed from tree structure)
- Dependencies selector (dropdown of existing work items)
- Estimated effort input
- Tags input (multi-select or comma-separated)
- Auto-generate work item key (WI-001, WI-002, etc.)

### 4. AI Refinement Integration

Following the Introduction Card Pattern, with enhanced deliverable structure generation:

```html
<!-- Work Item Editor with AI Sparkle Button -->
<div class="box" id="workitem-editor-box">
  <div id="workitem-content">
    <div class="field">
      <label class="label">Description</label>
      <textarea id="workitem-description" class="textarea" rows="4"></textarea>
    </div>
    
    <div class="field">
      <label class="label">Deliverables Structure</label>
      <!-- 🆕 Tree Builder Component -->
      <div id="deliverables-tree-builder">
        <!-- Tree view with folders and files -->
      </div>
    </div>
    
    <div class="buttons is-right">
      <button class="button is-primary" onclick="saveWorkItem()">
        <span class="icon"><i class="fas fa-save"></i></span>
        <span>Save</span>
      </button>
      
      <!-- AI Refine Button (HTMX powered) -->
      <!-- 🆕 AI can now suggest folder structures and prompt instructions -->
      <button class="button is-info"
              hx-post="/api/v1/agencies/{id}/work-items/{key}/refine"
              hx-include="#workitem-description, #deliverables-tree-builder"
              hx-target="#workitem-content"
              hx-indicator="#ai-process-status">
        <span class="icon"><i class="fas fa-magic"></i></span>
        <span>Refine with AI</span>
      </button>
    </div>
  </div>
</div>
```

**AI Enhancement Features**:
- AI suggests appropriate folder structures based on work item type
- Generates prompt instructions for each deliverable node
- Recommends file organization patterns (e.g., introduction/, requirements/, architecture/)
- Provides example content templates per file type

### 5. Dependencies Visualization

- Visual dependency graph
- Dependency validation (prevent circular dependencies)
- Highlight blocking work items
- Suggest logical sequencing

### 6. Backend Services

- `WorkItemService` interface and implementation
- CRUD operations with agency-scoped access
- Auto-numbering for work item keys
- Dependency cycle detection
- Work item templates management
- Export service (PDF, Markdown, JSON generation)
- **🆕 DeliverableValidator** - Tree validation with constraints
- **🆕 PromptComposer** - Hierarchical prompt instruction composition for AI agents
- **🆕 PathComputer** - Auto-compute full paths from tree structure

**Prompt Composition Service**:
```go
type PromptComposer interface {
    // Compose hierarchical prompt from deliverable node and its ancestors
    ComposePrompt(node *DeliverableNode) (string, error)
}

// Example Output Format:
// PROJECT GUIDELINES:
// [Root deliverable prompt instructions]
//
// FOLDER GUIDELINES:
// [Parent folder prompt instructions]
//
// YOUR TASK:
// [Specific file prompt instructions]
```

### 7. API Endpoints

```
GET    /api/v1/agencies/{id}/work-items
POST   /api/v1/agencies/{id}/work-items
PUT    /api/v1/agencies/{id}/work-items/{key}
DELETE /api/v1/agencies/{id}/work-items/{key}
POST   /api/v1/agencies/{id}/work-items/{key}/refine      # AI refinement (includes deliverable structure)
GET    /api/v1/agencies/{id}/work-items/templates         # Work item templates
POST   /api/v1/agencies/{id}/work-items/validate-deps     # Validate dependencies
GET    /api/v1/agencies/{id}/work-items/export/pdf        # Export to PDF
GET    /api/v1/agencies/{id}/work-items/export/markdown   # Export to Markdown
GET    /api/v1/agencies/{id}/work-items/export/json       # Export to JSON

# 🆕 Deliverable Tree Management
POST   /api/v1/agencies/{id}/work-items/{key}/deliverables/validate  # Validate deliverable tree
POST   /api/v1/agencies/{id}/work-items/{key}/deliverables/compose-prompt/{nodeId}  # Get AI prompt for node
```

### 8. Template System

- Predefined work item templates by type
- **🆕 Templates include deliverable folder structures with prompt instructions**
- Context-aware template suggestions
- Save custom templates

**Example Template Structure**:
```json
{
  "template_name": "Software Requirements Work Item",
  "work_item": {
    "type": "Documentation",
    "deliverables": [
      {
        "name": "Software Requirements",
        "type": "folder",
        "path": "documents/1-SoftwareRequirements",
        "prompt_instructions": "Create professional software requirements documentation following IEEE 830 standards. Use formal tone.",
        "children": [
          {
            "name": "introduction",
            "type": "folder",
            "prompt_instructions": "Cover project background, stakeholders, and objectives. Each document should be comprehensive but concise.",
            "children": [
              {
                "name": "stakeholders.md",
                "type": "file",
                "prompt_instructions": "List all stakeholders with roles, influence levels, and key interests. Use markdown tables."
              },
              {
                "name": "vision.md",
                "type": "file",
                "prompt_instructions": "Document the project vision, goals, and success criteria."
              }
            ]
          }
        ]
      }
    ]
  }
}
```

### 9. Export Functionality

#### Export to PDF
- Professional formatted document
- Table of work items with all details
- Dependency graph visualization
- Agency branding (logo, colors)
- Page numbers and table of contents

#### Export to Markdown
- Well-structured markdown format
- GitHub/GitLab compatible
- Work items as tables
- Dependencies as lists
- Easy to version control

#### Export to JSON
- Complete data export
- Structured format for API integration
- Includes all metadata
- Import/backup capability

#### Export UI
- Export button with dropdown menu
- Select format (PDF/Markdown/JSON)
- Preview before export
- Batch export multiple work items

---

## Acceptance Criteria

- [ ] Users can create, edit, and delete work items
- [ ] Work item keys auto-generate uniquely (WI-001, WI-002, etc.)
- [ ] Filtering and search work correctly
- [ ] Dependencies can be added and validated
- [ ] **🆕 Deliverable tree builder UI functional (add/remove/nest folders and files)**
- [ ] **🆕 Drag-and-drop reordering and nesting works**
- [ ] **🆕 Inline editing of names, descriptions, and prompt instructions**
- [ ] **🆕 Path auto-computation from tree structure**
- [ ] **🆕 Tree validation enforces constraints (max depth, name length, etc.)**
- [ ] **🆕 Prompt composition generates hierarchical instructions for AI**
- [ ] AI refinement improves descriptions and suggests deliverable structures
- [ ] No circular dependencies allowed
- [ ] Work items are agency-scoped
- [ ] Real-time validation feedback
- [ ] Export to PDF generates professional document (including deliverable tree)
- [ ] Export to Markdown creates valid markdown file
- [ ] Export to JSON produces valid JSON structure

---

## Implementation Notes

**File Locations**:
```
/workspaces/CodeValdCortex/
├── internal/
│   ├── agency/
│   │   ├── models/
│   │   │   ├── work_item.go (updated with DeliverableNode)
│   │   │   └── deliverable_node.go (new - tree structure)
│   │   ├── services/
│   │   │   ├── work_item_service.go
│   │   │   ├── deliverable_validator.go (new - validation)
│   │   │   └── prompt_composer.go (new - AI prompt composition)
│   │   └── work_item_repository.go
│   ├── handlers/
│   │   └── agency_handler_work_items.go
│   └── web/
│       └── pages/
│           └── agency_designer/
│               ├── work_items_list_card.templ
│               ├── work_item_editor_card.templ (updated)
│               ├── deliverable_tree_builder.templ (new)
│               └── agency_designer_work_items.templ
└── static/
    ├── css/
    │   └── work-items.css
    └── js/
        └── agency-designer/
            ├── work-items.js
            └── deliverable-tree.js (new - tree UI logic)
```

**Integration Points**:
- Reuses AI refinement pattern from MVP-029 (Goals Module)
- Integrates with agency context and selection
- Connects to export service infrastructure
- **🆕 Prompt composition service used by agent execution engine**
- **🆕 Deliverable validation integrated with work item CRUD**
- **🆕 Tree builder component reusable in other contexts**

**Validation Rules**:
- Max nesting depth: 10 levels
- Max name length: 255 characters
- Max prompt instructions: 5000 characters
- Max children per node: 100
- Files must end with `.md` extension
- No duplicate paths within work item
- Valid path characters (alphanumeric, dash, underscore, slash)
- Required fields: name, type
- Prompt instructions recommended (supports inheritance)

**Prompt Composition Example**:
For file `documents/1-SoftwareRequirements/introduction/stakeholders.md`:
```
PROJECT GUIDELINES:
Create professional software requirements documentation following IEEE 830 standards. Use formal tone.

FOLDER GUIDELINES (introduction/):
Cover project background, stakeholders, and objectives. Each document should be comprehensive but concise.

YOUR TASK (stakeholders.md):
List all stakeholders with roles, influence levels, and key interests. Use markdown tables.
```
